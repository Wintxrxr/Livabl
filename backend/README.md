## Livabl Backend

FastAPI service for ward data and scoring endpoints.

### Run locally

```bash
cd backend
uv sync --dev
uv run python run.py
```

Server:
- `http://127.0.0.1:8000`
- health endpoint: `GET /health`
